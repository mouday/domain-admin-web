import { createEnumObject } from '@/utils/enums-util.js'

// 枚举值，用于逻辑判断
export const NotifyTypeEnum = {
  // 未知
  Unknown: 0,
  // 邮件
  Email: 1,
  // webkook
  Webkook: 2,
  // 企业微信
  WorkWeixin: 3,
}
